# HexStrike AI — Top-level requirements
# For Codespaces: pip install -r requirements.txt
# For Docker: installed via Dockerfile

# AI Models
google-generativeai>=0.8.0
mistralai>=1.0.0

# Gradio Dashboard
gradio>=6.0.0

# HexStrike Server dependencies (also in hexstrike/requirements.txt)
flask>=2.3.0
psutil>=5.9.0
requests>=2.31.0
aiohttp>=3.8.0
beautifulsoup4>=4.12.0
mcp>=0.9.0
